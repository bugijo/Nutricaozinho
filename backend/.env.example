# Conexão PostgreSQL (qualquer provedor: Docker local, Neon, Railway, etc).
# Para o docker-compose incluído na raiz do projeto, use a URL abaixo:
DATABASE_URL="postgresql://nutri:nutri@localhost:5432/nutricaozinho?schema=public"

# Porta do servidor da API
PORT=3333
